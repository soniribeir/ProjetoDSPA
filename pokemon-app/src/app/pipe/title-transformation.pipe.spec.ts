import { TitleTransformationPipe } from './title-transformation.pipe';

describe('TitleTransformationPipe', () => {
  it('create an instance', () => {
    const pipe = new TitleTransformationPipe();
    expect(pipe).toBeTruthy();
  });
});
